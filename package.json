{
  "name": "easy-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run src",
    "test:watch": "vitest",
    "d1:migrate:local": "wrangler d1 migrations apply wordward-preview --env preview --local",
    "d1:migrate:preview": "wrangler d1 migrations apply wordward-preview --env preview --remote",
    "d1:migrate:production": "wrangler d1 migrations apply wordward-production --env production --remote",
    "d1:test": "vitest run worker/db",
    "worker:test": "vitest run worker",
    "worker:dev": "npm run build && wrangler dev --env preview --local",
    "worker:smoke": "node scripts/worker-smoke.mjs",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy --env production",
    "verify:worker": "npm test && npm run worker:test && npm run build && wrangler deploy --dry-run --env preview",
    "release:check": "node scripts/release-check.mjs"
  },
  "devDependencies": {
    "vite": "8.2.0",
    "vitest": "4.1.10",
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "hono": "^4.13.0"
  }
}
